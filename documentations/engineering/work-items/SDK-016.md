<!-- ce-bootstrap:SDK-016 -->

## SDK-016 — Qualify assembly and disassembly contracts by instruction profile

**Owner:** `CheatEngineNet/CheatEngine.SDK` · **Kind:** research · **Priority:** P2 · **Milestone:** SDK-M4

**Implementation status:** Complete. **Runtime validation:** No live Cheat Engine session was executed.

### Context and evidence

Prior ABI review identifies conflicts in address widths and disassembly-related signatures. No independent assembler/disassembler backend is requested.

**Evidence classification:** InheritedAuditBasis + Proposal; revalidate the pinned call path before implementation.

### Outcome and rationale

Define precise CE instruction input/output contracts with qualified target architecture and buffer rules.

**Expected benefit:** Define precise CE instruction input/output contracts with qualified target architecture and buffer rules.

### Architectural responsibility

SDK owns CE mappings, native safety, factual outcomes and low-level owners. Client owns application policy, typed workflows, composition, and developer experience.

### Technical requirements and task checklist

- [x] Separate assembler, disassembler, instruction navigation and Auto Assembler script semantics.
- [x] Record address widths, capacity/length, failure shape, text encoding and relative-address interpretation.
- [x] Keep unresolved functions unavailable instead of guessing native signatures.

### Acceptance criteria

- [x] Each exposed operation points to primary implementation evidence and native/managed conformance.
- [x] Target architecture is explicit and output lifetime is bounded.
- [x] Client need not parse UI text to repair a native contract.

### Required validation

- [x] Known instruction corpus and boundary-address cases.
- [x] Invalid instruction, capacity failure and target-width mismatch fixtures.

### Scope exclusions

- No unrelated rewrite, automatic capability activation, package publication, or repository-protection changes.
- Keep integration mechanisms in SDK and application policy/workflows in Client.

### Parent and children

Parent: https://github.com/CheatEngineNet/CheatEngine.SDK/issues/26

Implementation leaf. Keep small tasks in this checklist; split a new issue only for an independently reviewable outcome.

### Blocked by

- https://github.com/CheatEngineNet/CheatEngine.SDK/issues/32
- https://github.com/CheatEngineNet/CheatEngine.SDK/issues/41

### Blocks

None declared. This does not waive evidence, policy, or package requirements.

### Package and readiness gate

Identify the first containing artifact before describing new source as consumable support. No version is invented by this plan.

### Proposed branch and PR

Branch: `feat/sdk-016-instruction-contracts`

PR title: **Qualify assembly and disassembly contracts by instruction profile**

Open against `main` only after refinement and prerequisite review. Add `Closes <this issue URL>` for this leaf when the PR actually completes it. Reference upstream SDK work as a dependency, not as an issue to close. The bootstrap does not create this implementation branch.

### Risks and compatibility

Wrong signatures or implied relocation support can silently produce incorrect instructions. This task may close a specific profile as deferred.

**Long-term impact:** One traceable owner, stable acceptance criteria, and explicit compatibility evidence reduce repeated integration fixes and make future API changes reviewable.

### Definition of done

- [ ] Focused PR linked to the implementation issue.
- [x] Commands, environment and actual regression results; unexecuted cases remain pending.
- [x] Updated contract/capability documentation and compatibility notes.

Required for the affected capability. Optional profiles may be deferred through an explicit ADR, not marked as implemented.

### Sources

- [U02 — Historical Pascal implementations versus C declarations.](https://github.com/cheat-engine/cheat-engine/blob/ec45d5f47f92a239ba0bf51ec5d04a7509c3fd37/Cheat%20Engine/pluginexports.pas)
- [U03 — Classic declarations; pointer slots do not all denote direct functions.](https://github.com/cheat-engine/cheat-engine/blob/ec45d5f47f92a239ba0bf51ec5d04a7509c3fd37/Cheat%20Engine/plugin/cepluginsdk.h)
- [S10 — ABI scope reference, not certification of every table slot.](https://github.com/CheatEngineNet/CheatEngine.SDK/blob/aa3fcc3cdf629468e69d0c68817183d44a719894/libs/CheatEngine.SDK.Abi/README.md)

### Maintainer notes

Keep execution updates, actual commands/results, decisions, and refinements here. The bootstrap does not overwrite an existing issue body on rerun.

- 2026-09-21: implementation maps only the protected Lua globals `assemble`, `disassemble`,
  `splitDisassembledString`, `getInstructionSize`, `getPreviousOpcode`, `getOpenedProcessID`, and target ISA probes.
  `AutoAssemblerPatcher` remains a separate disable-info owner. The classic `Assembler`, `Disassembler`,
  `disassembleEx`, `previousOpcode`, and `nextOpcode` slots stay opaque: the source-indexed conflicts record the
  NUL-capacity, address-indirection, and 32/64-bit return-width disagreements.
- 2026-09-21: `InstructionProfiles` observes PID/probe/PID and instruction operations recheck PID before and after
  CE work. This detects an observed selection change but is not a selection lock, target-incarnation qualification,
  relocation guarantee, or proof against an unseen A→B→A transition.
- 2026-09-21: `InstructionOperationsTests` passed 11/11 through the native Lua fixture. It covers x86/x64/ARM width
  facts, relative origin forwarding, complete-or-no-prefix assembly copy, rejection, malformed results, raw UTF-8
  line and collective split-field bounds before decoding, copied parsed disassembly fields, strict numeric navigation,
  and target changes. It does not execute CE.
- 2026-09-21: after rebase on `origin/main` `050d8f3`, locked restore passed. Non-incremental Debug and Release
  solution builds both passed with zero warnings and zero errors using `UseSharedCompilation=false`; this avoids the
  stale Roslyn incremental artifacts previously seen during concurrent builds. Debug and Release solution tests each
  passed 2,216 tests with zero failures or skips. `dotnet pack` produced
  `CheatEngine.SDK.1.0.1-alpha.0.26.nupkg`; the Native AOT probe published and executed successfully. These build
  validations prove managed/package compatibility only, not CE-host plugin loading or a live target qualification.
