<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Does not ship on its own. Code the CESDK generators and analyzers have in common: pipeline and emission helpers, the Lua call emitters, the plugin shape predicate and the LuaBindings input validation. Packed beside the components under analyzers/dotnet/cs.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- The types stay internal: only the Roslyn components and their tests see them. -->
    <InternalsVisibleTo Include="CESDK.SourceGenerators.EntryPoint"/>
    <InternalsVisibleTo Include="CESDK.SourceGenerators.LuaBindings"/>
    <InternalsVisibleTo Include="CESDK.SourceGenerators.EngineApi"/>
    <InternalsVisibleTo Include="CESDK.Analyzers"/>
    <InternalsVisibleTo Include="CESDK.Analyzers.CodeFixes"/>
    <InternalsVisibleTo Include="CESDK.SourceGenerators.EntryPoint.Tests"/>
    <InternalsVisibleTo Include="CESDK.SourceGenerators.LuaBindings.Tests"/>
    <InternalsVisibleTo Include="CESDK.SourceGenerators.EngineApi.Tests"/>
    <InternalsVisibleTo Include="CESDK.Analyzers.Tests"/>
  </ItemGroup>

</Project>