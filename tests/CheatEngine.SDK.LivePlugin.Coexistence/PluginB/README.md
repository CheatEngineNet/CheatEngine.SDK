# Coexistence Plugin B

The second assembly in the opt-in [two-plugin coexistence fixture](../README.md). It has a unique plugin identity and
Lua names so the controlled Cheat Engine run can distinguish it from Plugin A. Build and live-host qualification steps
are documented at the fixture root; this project is not a CI test and has no recorded live result.
