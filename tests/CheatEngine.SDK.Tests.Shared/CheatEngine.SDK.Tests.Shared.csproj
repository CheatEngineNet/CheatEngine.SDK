<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Test support only: it runs no test and ships nowhere. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../libs/CheatEngine.SDK.Lua.Interop/CheatEngine.SDK.Lua.Interop.csproj"/>
  </ItemGroup>

  <ItemGroup>
    <!-- Cheat Engine's own Lua, flowing into the output of every project that references this one (native/cheat-engine/README.md). -->
    <None Include="$(RepoRoot)native/cheat-engine/lua53-64.dll" Link="native/lua53-64.dll" Visible="false"
          CopyToOutputDirectory="PreserveNewest"/>
  </ItemGroup>

  <ItemGroup>
    <!-- The fixture types stay internal: only the projects that use them see them. -->
    <InternalsVisibleTo Include="CheatEngine.SDK.Lua.Interop.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.Lua.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.Engine.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.Hosting.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.SourceGenerators.EngineApi.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.SourceGenerators.LuaBindings.Tests"/>
    <InternalsVisibleTo Include="CheatEngine.SDK.Benchmarks"/>
  </ItemGroup>

</Project>